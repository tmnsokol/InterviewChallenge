# Invoice Processing System

This project calculates invoice totals including tax and discount.


How to run calculations:

You can run calculations from main.py
```
    result = process_invoice(
        price=100,
        quantity=50,
        tax_rate_percent=50,
        discount_percent=15
    )

```